# CIPC LabEquip Hub v1.2.1 发布说明

## 修复

- 总览问候语不再固定显示“早上好”。
- 根据浏览器本地时间显示“夜深了、早上好、中午好、下午好、晚上好”。
- 页面跨过时间段边界时自动更新问候语。
- 标签页从后台恢复时重新校准问候语，避免设备休眠后显示过期内容。

## 验证

- 使用纯函数回归测试覆盖 0-23 时的全部问候时间段边界。
- 通过完整语法检查和测试套件。
- 发布前创建并校验生产 SQLite 一致性快照。
